<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ .Site.Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ .Site.Params.description | default .Site.Title }}</description>
    <generator>Hugo</generator>
    <language>{{ .Site.LanguageCode }}</language>
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    <atom:link href="{{ .Permalink }}" rel="self" type="application/rss+xml" />
    {{/* merge the three sections into one slice, then range */}}
    {{ $pages := union
        (where .Site.RegularPages "Section" "blog")
        (union
          (where .Site.RegularPages "Section" "notes")
          (where .Site.RegularPages "Section" "photos")
        )
    }}
    {{ range first 10 $pages.ByDate.Reverse}}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .PublishDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <author>wickedlyethan</author>
      <guid isPermaLink="true">{{ .Permalink }}</guid>
      {{ if and (eq .Section "photos") .Params.Photos }}
        <enclosure url="{{ (index .Params.Photos 0).src | absURL }}" type="image" />
      {{ end }}
      <description>
        {{ .Summary | transform.XMLEscape | safeHTML }} <p><a href="{{ .Permalink }}">Read more →</a></p>
      </description>
    </item>
    {{ end }}
  </channel>
</rss>
